- [ ] `auth_page.dart`: validate all the fields after submition (first app validation, then Firebase). And show error messages only at the bottom (one at a time). After pressing submit, validate first, if fine, then call Firestore, and if asign a single error message to show at the bottom.

- [ ] `auth_page.dart`: Create a custom elevated button with a MAX height (maybe a container)

- [ ] `auth_page_model.dart`: Consider adding a symbol check on the password when scaling a bit.

- [ ] Create a Command object with 'error' and 'isRunning' and maybe 'isSuccessful'

- [ ] add logs in Authentication logic (view models probably)